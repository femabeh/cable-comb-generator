import { Routes } from '@angular/router';
import {CableCombComponent} from './components/cable-comb/cable-comb.component';

export const routes: Routes = [
  { path: '', component: CableCombComponent },
];
